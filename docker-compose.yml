version: '3.8'

services:
  postgres:
    build: ./docker/postgres
    environment:
      POSTGRES_DB: marketinsights
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  postgres_data: